# 크로스 앱 라우터 실동작 구현

- 작성일: 2026-08-24
- 상태: 제안 — 에이전트가 작성했고 사용자가 아직 확정으로 올리지 않았다
- 의도 확인: 미확인
- 문서 유형: 종료 시점 스냅샷 — 2026-08-28 프로젝트 종료 이후 갱신하지 않는다
- 작성자: Claude Code (사용자 판단을 옮겨 적음)

## 결정 일자

2026-08-24

## 결정 주제

제출용 결과물을 화면 전환 목업이 아니라 **실제로 동작하는 크로스 앱 라우터**로 만든다. 이 결정은 [`기능콘셉트_범용도구화.md`](./기능콘셉트_범용도구화.md)에서 작업량을 이유로 기각했던 `완전 범용화` 대안을 되살리는 것이다.

## 배경

2026-08-21에는 세 가지 대안 중 `가벼운 범용 프레이밍`을 채택하고, `회사가 앱·기능·매핑 규칙을 입력하면 실제로 크로스앱 안내가 자동 생성되는 규칙엔진까지 구현`하는 안은 **마감까지 6일, 작업량이 너무 큼**을 이유로 기각했다.

2026-08-24에 사용자가 "실제로 작동하는 개념의 서비스를 만들고 싶다"고 방향을 바꿨다. 동시에 설계 제약을 하나 제시했다 — **계열사 앱이 부담하는 리소스를 최대한 줄일 것.**

## 왜 이번에는 가능한가

기각 당시의 `규칙엔진`과 지금 만든 것은 범위가 다르다.

| 2026-08-21에 상상한 것 | 2026-08-24에 만든 것 |
|---|---|
| 검색 결과를 자동 생성하는 규칙 엔진 | 검색은 손대지 않음. 담당 채널 기준만 배포 |
| 서버·DB·권한 시스템 | 서버 없음. 브라우저 저장소로 연동 |
| 매칭·랭킹 로직 | 정규화된 사전 조회 한 번 |

범위를 좁히니 이틀 안에 들어왔다. **기각 판단이 틀렸다기보다, 기각 대상이던 범위와 지금 범위가 다르다.**

## 검토한 선택지

조회 구조에는 두 갈래가 있었다. **(2026-08-25 추가)**

### 채택 — 명부를 배포한다

중앙이 승인된 기준을 스냅샷으로 발행하고, 앱이 하루 1회 받아 두었다가 검색할 때 자기 안에서 찾는다. 지금 구현이 이쪽이다.

### 기각 — 앱이 중앙에 직접 물어본다

앱이 검색어를 중앙에 보내고 중앙이 해당 안내만 돌려준다. 앱은 명부를 갖지 않는다.

| | 얻는 것 | 잃는 것 |
|---|---|---|
| 배포 | 명부 배포·동기화가 통째로 사라진다. 기준이 아무리 커져도 앱은 그대로다 | — |
| 로그 | 0건 검색 로그가 저절로 쌓인다. 계열사가 따로 내보내 올릴 필요가 없다 | — |
| 반영 | 기준을 바꾸면 즉시 반영된다 (지금은 최대 하루 지연) | — |
| 네트워크 | — | 검색당 0 B가 사라지고 매 검색이 왕복을 기다린다 |
| 장애 | — | 중앙이 멈추면 계열사 앱의 검색 안내도 함께 멈춘다 |
| 개인정보 | — | 사용자가 입력한 검색어가 실시간으로 다른 법인에 넘어간다 |

**왜 기각했는가.** 계열사가 감독 체계가 다른 별도 법인이라는 것이 이 프로젝트의 전제다. 잃는 것 세 가지는 모두 계열사 보안·심사에서 거부될 수 있는 항목이고, `도입에 손이 많이 가면 채택되지 않는다`는 이 설계의 성립 조건과 정면으로 부딪힌다. 특히 금융 앱 검색창에는 `채무조정`·`상속` 같은 입력이 들어오므로, 별도 법인 간 실시간 전송은 개인정보 처리 위탁·제3자 제공 검토 대상이 된다.

현재 설계는 검색어를 **0건이 난 것만, 집계된 형태로, 사후에** 받는다.

**언제라면 기각한 쪽이 맞는가.** 한 회사가 자기 앱들만 상대하는 경우다. 그때는 위의 `잃는 것`이 대부분 성립하지 않으므로 중앙 조회 방식이 더 낫다.

**남은 확장 여지.** 명부가 커질 때를 대비해 두 방식을 섞을 수 있다. 중앙이 전체 대신 **앱마다 필요한 부분만 잘라 보내는** 것이다. 판단이 다시 중앙으로 가므로 이 설계의 원칙과 어긋나지 않고, 스냅샷에 이미 있는 `suppressIn`(어느 앱에서 안내를 숨길지)과 개념이 이어진다. 이번 마감 범위 밖이며 후속 과제로 남긴다.

## 최종 결정

`prototype/router/`에 실제로 동작하는 구현을 둔다.

```text
[계열사 담당자]      [라우터 콘솔]           [발행 스냅샷]          [계열사 앱]
상품·업무 데이터  →  자동 매핑           →  routing-index.json →  routing-client.js
내보내기(CSV)        역할·다음 행동만 수기     version + etag        sync()  하루 1회
                     정규화·표현 확장         index / functions     lookup() 검색마다
                     미노출 지정              (승인된 것만)          네트워크 0
                     중앙 승인 후 발행
```

핵심 설계는 **지능을 중앙에 두고 말단은 사전 조회만 하게 하는 것**이다. 앱은 동의어 판단도, 담당 채널 판단도, 어느 앱에서 안내를 숨길지도 결정하지 않는다.

### 등록은 양방향이다

- **밀어 넣기**: 계열사가 이미 가진 데이터를 내보내 올린다. 회사 데이터에 없어서 손으로 채우는 값은 `채널 역할`과 `다음 행동` 두 가지뿐이다.
- **끌어오기**: 앱의 검색 결과 0건 로그를 올리면 라우터가 등록할 표현을 역으로 찾아낸다. 승인된 기준에 표현을 추가하면 상태가 `검토 중`으로 되돌아가 재승인을 받는다.

## 결정 이유

- 계열사가 감독 체계가 다른 별도 법인이므로, 도입에 손이 많이 가면 실제로 채택되지 않는다. `앱은 조회만, 계열사는 내보내기만`이 이 도구가 성립하는 조건이다.
- 과제 평가의 `동작·완성도`(20점)와 제출물 요건 `동작하는 결과물`을 목업으로는 채우기 어렵다.
- 담당 채널은 검색 데이터가 아니라 조직 데이터다. 어느 검색 엔진 사전에도 그 값이 없으므로 별도 기준이 필요하다는 논지가 구현으로 증명된다.

## 여전히 하지 않는 것

- 실제 NH 앱·백엔드·거래·로그인·딥링크 구현
- 검색 엔진의 색인·랭킹·동의어 사전 등 검색 품질 자체
- 권한이 분리된 승인 워크플로와 감사 로그
- 여러 기기·여러 사람 간 공유 (서버가 없다)

## 예상 영향

- [`B축_MVP_설계.md`](../B축_MVP_설계.md)의 "실제 관리자 권한·데이터 동기화는 구현하지 않는다"가 사실과 달라진다. 데이터 동기화는 구현했고 권한은 여전히 미구현이다.
- [`ThinkCode_시작프롬프트.md`](../../_done/docs/ThinkCode_시작프롬프트.md)의 기술 스펙 `단일 HTML 파일`이 3개 파일 + 샘플 구조로 바뀐다.
- 기존 프로토타입 3종은 수정·삭제하지 않고 그대로 보존한다.
- **사용자 효과는 여전히 미검증이다.** 안내형 After 사용성 테스트를 이번 마감에 포함하지 않기로 2026-08-24에 결정했다. 제시할 수 있는 수치는 커버리지와 도입 비용이며 사용자 효과가 아니다.

## 관련 문서

- [`prototype/router/README.md`](../../prototype/router/README.md) — 실행 방법, 스키마, 프로덕션 대응표, 측정값
- [`기능콘셉트_범용도구화.md`](./기능콘셉트_범용도구화.md) — 이 결정이 되살린 대안이 기각됐던 기록
- [`사용성테스트_프로토타입_제작방식.md`](./사용성테스트_프로토타입_제작방식.md) — 기존 프로토타입의 위치
- [`인앱검색_도메인학습기록.md`](../인앱검색_도메인학습기록.md) — 이 설계의 개념적 근거
- [`계열사_도입유인.md`](./계열사_도입유인.md) — 계열사가 이 안내를 도입할 유인에 대한 판단

## 근거 원본 (raw)

- [[1차 프로젝트 가이드]]
