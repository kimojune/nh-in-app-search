# AI_Workspace 공통 작업 규칙

이 문서는 Claude Code와 Codex를 포함해 이 작업공간에서 일하는 모든 에이전트가 따르는 공통 규칙의 단일 기준이다. 에이전트별 진입 파일에는 이 내용을 복제하지 않고 이 문서를 참조한다.

## 언어와 형식

- 기본 언어는 한국어를 사용한다.
- 문서는 Markdown 형식으로 작성한다.
- 핵심 결론을 먼저 쓰고, 근거와 세부 내용은 뒤에 설명한다.
- 사실, 해석, 추정을 명확히 구분한다.

## 요청 범위와 작업 권한

- 설명·검토·진단·계획 요청에서는 관련 자료를 읽고 결과만 보고한다. 파일 변경은 하지 않는다.
- 변경·작성·수정 요청에서는 요청 범위 안의 파일만 변경하고 비파괴적 검증을 수행한다.
- 외부 공개, 외부 서비스 쓰기, 파괴적 작업, 비용 발생 또는 작업 범위의 실질적 확대에는 사용자 확인이 필요하다.

## 작업 전 확인

- 관련 폴더와 기존 파일을 먼저 확인한다.
- `git status --short`로 다른 에이전트나 사용자가 남긴 변경을 확인한다. 원격에 새 커밋이 있을 수 있으니 `git fetch`로 origin 상태도 함께 확인한다.
- 프로젝트의 `PROJECT_RULES.md`, `PROJECT_GUIDE.md`, `ACTIVE_WORK.md`, `HANDOFF.md`가 있으면 읽는다.
- `scripts/check-docs-consistency.ps1`을 실행해 깨진 상대경로 `.md` 링크나 `decision-log/README.md` 색인 누락이 이미 있는지 먼저 확인한다. LLM 호출 없는 순수 스크립트라 비용이 거의 들지 않는다.
- `ACTIVE_WORK.md`에 다른 에이전트가 진행 중인 작업이 있으면 같은 파일을 수정하지 않는다. 충돌하지 않는 읽기·검토만 수행하거나 사용자에게 작업 전환을 확인한다.
- 기존 문서를 우선 활용하고 불필요한 중복 파일을 만들지 않는다.
- 요청이 모호하고 중요한 결과가 달라질 수 있으면 임의로 결정하지 않는다.
- 여러 세션에 걸친 프로젝트는 `PROJECT_GUIDE.md`와 `docs/`에서 `미확인`, `검토 필요`, `예정`, `추가정보필요` 항목을 확인한다.
- 같은 사실을 여러 문서에 중복 기록하지 않는다. 하나의 단일 기준을 정하고 다른 문서에서는 링크로 참조한다.
- `docs/decision-log/`의 결정을 근거로 다른 문서를 작성하거나 외부에 설명할 때, 그 결정 문서의 `의도 확인` 필드가 `미확인`이면 먼저 사용자에게 알린다. 이 필드는 사용자가 그 문서를 읽고 자기 의도와 맞는지 확인했는지를 기록하며, 값은 `미확인` / `확인함 (YYYY-MM-DD)` / `불일치 — 무엇이 다른지` 셋이다. 확인 없이 그대로 옮겨 적으면 의도와 어긋난 결론이 확정 사실로 퍼진다. 이 필드를 채우는 것은 사용자만 한다.

## 변경과 인수인계

- 승인 없이 중요한 내용을 삭제하거나 덮어쓰지 않는다.
- 기존 문서를 대폭 변경해야 하면 변경 계획을 먼저 설명한다.
- 사용자가 직접 수정한 내용이나 다른 에이전트의 미완료 변경을 임의로 되돌리지 않는다.
- 파일 변경 후 무엇을 왜 바꿨는지와 검증 결과를 보고한다.
- 에이전트를 전환할 때 `ACTIVE_WORK.md`에 현재 상태를, `HANDOFF.md`에 완료·미완료·검증·주의사항을 기록한다.
- 확정된 프로젝트 사실은 `HANDOFF.md`에만 남기지 않고 `PROJECT_GUIDE.md` 또는 `docs/decision-log/`에 반영한다.

## 금지하는 파괴적 작업

- `git reset --hard`, `git clean -f/-fd/-xdf`, `git checkout -- <경로>`를 실행하지 않는다.
- 사용자 변경을 폐기하는 `git restore`, 강제 푸시, stash 삭제·초기화를 실행하지 않는다.
- 재귀적 강제 삭제나 복구가 어려운 대량 이동·덮어쓰기를 하지 않는다.
- `.env`, `*.pem`, `*.key` 등 비밀정보 파일을 업무상 명시적 필요와 승인 없이 읽지 않는다.
- 커밋은 사용자가 명시적으로 요청한 경우에만 수행한다.

## raw/ 변경 시 문서 반영

- `raw/`의 기존 원본 파일은 수정하지 않는다. 새 원본을 추가하는 것과 기존 원본을 고치는 것을 구분한다.
- `raw/` 추가를 포함한 커밋 전에는 관련 결정이 `docs/decision-log/`와 `PROJECT_GUIDE.md`에 반영됐는지 확인한다.
- 반영 대상은 파일이 아니라 결정 주제 단위로 판단한다. 기존 주제는 기존 문서를 갱신하고 완전히 새로운 주제일 때만 새 문서를 만든다.
- 새로운 확정 사실 없이 재검토 의문만 있으면 decision-log를 바꾸지 않고 회의 안건이나 미확인 항목으로 남긴다.
- `PROJECT_GUIDE.md`에는 상태가 실제로 바뀐 항목과 링크만 반영하고 상세 내용을 중복 작성하지 않는다.
- 실질적인 결정 내용의 오류를 발견하면 먼저 설명하고 확인받은 뒤 고친다. 링크·날짜 오타 같은 비실질적 오류만 즉시 수정할 수 있다.

## 파일과 내용 작성

- 새 문서는 적절한 프로젝트 하위 폴더에 저장한다.
- 파일명 규칙의 단일 기준은 `NAMING.md`다.
- 확정된 내용과 검토 중인 내용을 구분한다.
- `docs/decision-log/` 문서의 `상태`를 에이전트가 `확정`으로 올리지 않는다. 에이전트는 `제안`까지만 쓰고, `확정`은 사용자가 문서를 읽은 뒤 직접 바꾼다. 확인받지 않은 결론이 `확정`으로 기록되면 `PROJECT_GUIDE.md`·PRD·기능요구서로 그대로 퍼져서 나중에 되돌리기 어렵다.
- 출처가 있는 정보에는 출처나 확인 경로를 남긴다.

## 커밋할 때 자동으로 하는 것

커밋을 만들 때, 그 커밋에서 정한 것과 그 이유를 `20-29_Areas/23_작업관리/02_PRACTICES.md`에 덧붙인 뒤 같은 커밋에 포함한다. 사용자가 요청하지 않아도 한다.

- 적을 내용이 없으면 적지 않는다. "앞으로도 적용될 방식"이 정해진 커밋에서만 뽑고, 이번 한 번짜리 작업 내용은 적지 않는다.
- 항목 한 줄, 이유 한 줄, 날짜. 이유를 한 줄로 못 쓰겠으면 판단이 여러 개 섞인 것이니 항목을 쪼갠다.
- 한 프로젝트에만 적용되는 방식은 `## 프로젝트별` 아래 해당 프로젝트 섹션에, 워크스페이스 전체에 적용되는 방식은 주제 섹션에 쓴다. 병렬 worktree가 같은 자리에 동시에 덧붙여 병합 충돌이 나는 것을 줄이기 위해서다. 주제 섹션은 여전히 겹칠 수 있는데, 충돌이 나면 양쪽 항목을 모두 살린다.
- 커밋에서 확인되지 않는 것은 추측해서 쓰지 않는다.
- 이 파일이 다른 프로젝트의 변경과 같은 커밋에 들어가면 `check-workspace.ps1`이 "Changes span multiple projects"로 경고한다. 이 경우는 의도된 것이다.

## Git 작업 단위

- 변경 전후에 `git status --short`와 `git diff`를 확인한다.
- 여러 프로젝트의 변경을 한 커밋에 섞지 않는다.
- `git add .`와 `git add -A` 대신 대상 프로젝트나 파일 경로를 지정한다.
- 병렬 작업이 꼭 필요하면 같은 체크아웃을 공유하지 않고 별도 브랜치와 worktree를 사용하며, 같은 파일을 동시에 수정하지 않는다.  
  
## 작업하며 정한 것

  작업 중 정해진 방식과 그 이유는 20-29_Areas/23_작업관리/02_[PRACTICES.md](http://PRACTICES.md) 에 기록한다.

