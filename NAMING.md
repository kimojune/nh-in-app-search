# 문서 네이밍 규칙

이 문서는 이 저장소 문서 파일명의 **단일 기준(source of truth)**이다. 다른 문서에서는 네이밍 규칙을 중복 기록하지 않고 이 문서를 링크로 참조한다.

**이 저장소에서 실제로 쓰이는 규칙은 `docs/`·`docs/decision-log/`·`raw/`·폴더 인덱스·공통 규칙이다.** 아래에는 AI_Workspace에서 그대로 가져온 규칙 중 이 저장소에 대상이 없는 것도 있다 — `snapshots/`의 배포 문서(`UPDATE`)와 버전 규칙, 학습 기록 폴더(`study/`·`lessons/`), JD 번호가 그렇다. 참고로 남겨두되 이 규칙들 때문에 없는 폴더를 새로 만들지 않는다.

## 한눈에 보기

| 문서 종류                                       | 파일명 형식                         | 예시                                            |
| ------------------------------------------- | ------------------------------ | --------------------------------------------- |
| 팀원 공유용 현황+결정 배경 요약 (`snapshots/` 하위 폴더에 보관) | `YYMMDD_프로젝트명_UPDATE_Vx.xx.md` | `snapshots/260813_DiaryGuide_UPDATE_V1.00.md` |
| `docs/` 내부 문서 — 고정 스냅샷(그 시점 기록, 예: 회의록)     | `YYMMDD_내용.md`                 | `260812_회의록.md`                               |
| `docs/` 내부 문서 — 계속 갱신                       | `내용.md` (날짜 접두어 없음)            | `AI표시방식_검토.md`                                |
| `docs/decision-log/` 개별 결정 기록               | `결정주제.md` (날짜 접두어·상태 접미어 없음)  | `얼굴인식_미포함.md`                                  |
| `raw/` 원본 자료                                | 받은/생성된 그대로 (수정하지 않음)           | `260805 토스 인앱 문의 내역.md`                       |
| 폴더 인덱스                                      | 고정 이름 `README.md`              | `docs/README.md`, `docs/decision-log/README.md` |
| 프로젝트 상시 문서                                  | 고정 이름                          | `README.md`, `PROJECT_GUIDE.md`, `CLAUDE.md`  |

## 파일명에는 바뀌는 정보를 넣지 않는다

파일명에 얹은 정보는 바뀔 때 리네임이 필요하고, 리네임하지 않으면 거짓이 된다. 리네임하면 그 파일을 가리키는 링크가 전부 깨진다. 그래서 넣지 않는다.

- **날짜** — 계속 갱신되는 문서에는 접두어를 붙이지 않는다 (아래 "날짜는 `YYMMDD` 6자리" 참고)
- **JD 번호** — 카테고리 폴더까지만 매기고 파일에는 매기지 않는다 (자세한 기준은 root 저장소의 `01_폴더규칙.md`)
- **상태** — `_확정` 같은 접미어를 붙이지 않는다

상태는 파일명이 아니라 문서 안 `- 상태:` 줄에 둔다. 값과 표기 규칙은 각 폴더의 인덱스(`README.md`)를 따른다.

## 날짜는 `YYMMDD` 6자리로 통일한다 (파일명에 날짜를 넣는 경우)

파일명에 날짜를 넣을 때는 **항상 6자리**를 쓴다. 예: `260812_회의록.md`

문서 본문 안의 날짜(`작성일`, `최종 수정일`, 결정 일자 등)는 파일명이 아니므로 `YYYY-MM-DD` 형식을 그대로 쓴다. 즉 **파일명은 `260812_`, 본문은 `2026-08-12`**다.

### `docs/decision-log/`와 `docs/`의 "계속 갱신" 문서는 파일명에 날짜를 넣지 않는다

원래는 `docs/`·`docs/decision-log/` 전부에 `YYMMDD_` 접두어를 요구했으나, 실제로는 이 안의 문서 대부분이 "문서 유형: 계속 갱신"이라 생성일 접두어가 오히려 틀린 정보가 됐다(예: `260806_PRD.md`가 접두일 이후로도 계속 갱신되거나, 결정 문서의 파일명 날짜는 그대로인데 본문엔 나중 정정이 담기는 경우). 그래서 두 갈래로 나눈다:

- **고정 스냅샷**(그 시점 기록이 문서의 정체성인 것 — 회의록 등): 기존대로 `YYMMDD_` 접두어를 유지한다.
- **계속 갱신**(`docs/decision-log/` 전체, `docs/`의 조사·기획·정책 문서 등): 파일명에 날짜를 넣지 않는다. 시간 정보는 본문의 `최종 수정일`(일반 문서) 또는 `작성일`(decision-log, 최초 작성일+갱신 이력을 함께 적음) 필드가 단일 기준이다. decision-log는 내용이 바뀔 때 새 필드를 만들지 않고 `최종 결정` 문단 옆에 `(YYYY-MM-DD 갱신: ...)`을 바로 덧붙인다(root 저장소의 `31_템플릿/DECISION_LOG_TEMPLATE.md` 참고) — 파일명보다 이쪽이 이미 실제로 쓰이고 있던 방식이라 이를 공식 기준으로 삼는다.
- 이 구분은 각 문서 상단에 이미 표시하는 "문서 유형: 계속 갱신 / 고정 스냅샷" 태그를 그대로 따른다.
- **"문서 유형" 필드는 `docs/`(및 `docs/decision-log/`) 안에 사는 문서에만 필요하다** — 이 필드의 목적 자체가 "파일명에 날짜를 넣을지"를 정하는 것이기 때문이다. `PROJECT_GUIDE.md`, `snapshots/`의 `UPDATE`, `study/`·`lessons/`의 학습 기록은 각자 이미 정해진 별도의 이름 규칙을 쓰므로 이 필드가 필요 없다.

> 예외로 남아 있는 곳: 학습 프로젝트의 날짜 폴더(`WantedCamp`의 `lessons/YYYYMMDD/`, `PMStudy`의 `study/YYYYMMDD/`)는 아직 8자리다. 해당 폴더는 완료 후 읽기 전용으로 잠기는 구조라 별도로 정리한다. 이미 잠긴 폴더 내부의 개별 파일명도 소급해서 리네임하지 않고 공백을 그대로 허용한다(아래 "공통 규칙"의 공백 예외에 두 폴더 내부 파일도 포함).

## 버전 규칙

팀원에게 **배포하는 문서에만** 버전을 붙인다. 그 외 문서는 파일명에 버전을 붙이지 않고 git으로 관리한다.

- `V1.00`에서 시작한다.
- **새 버전은 사실이 바뀔 때마다 만들지 않고, 팀원에게 다시 보여줄 필요가 생겼을 때만 만든다.** 그 시점의 `PROJECT_GUIDE.md`·`docs/decision-log/` 내용을 그대로 반영해서 생성한다.
- 새 버전을 만들 때는 **세 곳을 함께 올린다**: 파일명의 날짜와 버전, 문서 안의 `버전`, 문서 안의 `갱신일`.
- 새 버전을 만들면 파일명이 바뀌므로, **이 문서를 가리키는 링크도 함께 고친다** — 최소한 프로젝트의 `README.md`가 배포 문서를 가리키는 링크를 확인한다. 이전 버전 파일은 지우지 않고 그 시점 스냅샷으로 남겨둔다.
- **모든 버전(현재 최신본 포함)을 프로젝트 루트가 아니라 `snapshots/` 하위 폴더에 둔다.** 루트에는 과거·현재 버전이 섞여 쌓이면 최신본을 찾기 어렵고, 최신본만 루트에 남기는 방식은 버전이 바뀔 때마다 파일을 옮기는 절차가 추가로 필요해 번거롭다 — 위치를 아예 고정하는 쪽이 더 단순하다.

### 왜 배포 문서만 예외인가

일반 원칙은 "파일명에 버전 번호를 붙이지 않는다"이다(`README.md`의 "문서 변경 이력 관리" 참고). 파일명 버전은 어느 쪽이 최신인지 헷갈리게 만들고, git이 이미 이력을 관리하기 때문이다.

다만 팀원에게 건네는 문서는 워크스페이스 밖으로 나가 git 이력에 접근할 수 없는 상태로 공유된다. 받는 쪽이 **이게 언제 기준의 내용인지** 파일명만 보고 알 수 있어야 하므로 예외를 둔다.

## 문서 종류 이름

- **`UPDATE`** — 팀원이 빠르게 확인하는 현황+결정 배경 요약. 한 줄 요약, 지금 어디까지 왔나, 정해진 것과 그 이유, 자주 받는 질문, 아직 안 정해진 것, 지금 막혀 있는 것, 뒤집힐 수 있는 결정, 역할별 할 일.

**새로운 사실을 담는 곳이 아니라 요약과 링크만 담는 얇은 레이어**다. 확정/미확인 정책의 단일 기준은 `PROJECT_GUIDE.md`, 개별 결정의 전체 근거는 `docs/decision-log/`의 각 파일이다.

**생성 방식**: `UPDATE`는 사실이 바뀔 때마다 갱신하는 상시 문서가 아니라, **팀원에게 다시 보여줄 필요가 생겼을 때 그 시점의 `PROJECT_GUIDE.md`·`docs/decision-log/`를 그대로 옮겨 새로 생성하는 스냅샷**이다. 기존 버전은 그 시점 기록으로 그대로 두고 손대지 않는다. 상태와 배경을 별도 문서로 나누면 결정 하나가 바뀔 때마다 여러 곳을 함께 고쳐야 해서 누락·모순이 쌓이기 쉬우므로, 한 문서로 유지한다.

## 공통 규칙

- 파일명은 내용을 바로 파악할 수 있도록 명확하게 쓴다.
- 구분자는 언더스코어(`_`)를 쓴다. 공백은 `raw/`의 원본 자료와 학습 기록 폴더(`study/`·`lessons/`) 내부 파일에만 예외적으로 허용한다(받은/생성된 그대로 두거나, 잠긴 뒤 손대지 않으므로).
- 프로젝트명은 폴더명과 똑같이 쓰되, **JD 번호 접두어는 폴더명에만 붙이고 프로젝트명에는 포함하지 않는다.** 폴더가 `24_DiaryGuide`여도 프로젝트명은 `DiaryGuide`이고, 배포 문서는 `260821_DiaryGuide_UPDATE_V1.02.pdf`처럼 번호 없이 쓴다. 번호는 위치를 가리키는 주소이지 이름의 일부가 아니다.
- 파일에는 JD 번호를 매기지 않는다. 번호는 카테고리 폴더까지만 붙인다(자세한 기준은 root 저장소의 `01_폴더규칙.md`).

## 관련 문서

이 저장소는 AI_Workspace에서 독립해 나왔다. 아래는 root 저장소(`kimojune/AI_Workspace`)에 있으며 이 저장소에는 없다.

- `README.md` — 워크스페이스 전체 구조와 문서 역할 구분
- `01_폴더규칙.md` — 어디에 넣을지 판단하는 기준, JD 번호 규칙
- `30-39_Resources/31_템플릿/` — 문서 서식 모음 (`UPDATE_TEMPLATE.md` 등)
